# Just a simple sorting usage.
# Create a function that returns the elements of the input-array / list in a sorted manner.

def sortme(names):
    return sorted(names)

print(sortme(["one", "two", "three"]))